[project]
name = "mcp-remote-control"
version = "0.1.0"
description = "AI-powered local network control for TVs and devices. Currently supports Roku TVs via ECP."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.23.3",
]
