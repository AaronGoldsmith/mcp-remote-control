[project]
name = "mcp-tv-control"
version = "0.1.0"
description = "Control Roku TVs on your local network using the External Control Protocol (ECP)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.23.3",
]
